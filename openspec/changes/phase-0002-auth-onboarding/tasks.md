# Tasks — Phase 0002 Authentication & Onboarding

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | 2,400–3,400 |
| 400-line budget risk | High |
| Chained PRs recommended by forecast | Yes |
| Owner delivery decision | Do not use chained PRs for this phase |
| Implementation branch | `feat/phase-0002-auth-onboarding` |
| Internal split | Risk checkpoints and small coherent commits on the current branch |
| Delivery strategy | ask-on-risk resolved by owner decision: single branch, no chained PRs, no push/merge/PR without explicit authorization |
| Chain strategy | not applicable |

Decision needed before apply: Resolved; apply was authorized for Phase 0002 on the current branch.
Chained PRs recommended by forecast: Yes, but owner explicitly declined chained PRs for Phase 0002.
Chain strategy: not applicable.
400-line budget risk: High.

**Execution status:** Phase 0002 is applying on `feat/phase-0002-auth-onboarding`. Checkpoints 1 and 2 are completed, reviewed, acknowledged, and committed. Checkpoint 3 — Auth/session boundary is completed, reviewed, and acknowledged; tasks `5.3`, `7.1`, and `7.2` are complete. Checkpoint 4A is in progress. The owner has now supplied the real avatar assets and stable keys; Checkpoint 4B UI remains unstarted and still requires explicit authorization. Do not push, merge, or open a PR without explicit authorization. When a task/checkpoint requires gentle-ai review, stop and request authorization before continuing.

## Critical Order

1. Confirm PocketBase 0.40.3 behavior with a disposable harness before code relies on it.
2. Establish and prove schema/rules/hooks isolation before browser-facing auth or onboarding writes.
3. Establish request-scoped session and same-origin boundaries before login, SSR, or protected routes.
4. Establish Checkpoint 4A server-side onboarding persistence and atomic Home seed before exposing final onboarding UI.
5. Establish Checkpoint 4B onboarding UI only after 4A is reviewed and the avatar asset gate is satisfied.
6. Do not implement visual avatar selection until Checkpoint 4B is explicitly authorized; owner assets and final stable keys are now available.

## Checkpoint 4A / 4B split

Checkpoint 4 remains **Onboarding + atomic Home seed**, split explicitly for implementation and review control.

### Checkpoint 4A — Onboarding persistence + atomic Home seed

Includes:

- server-side onboarding contract and validation;
- profile persistence;
- `user_preferences`;
- timezone persistence/validation;
- nullable/unconfigured location persistence/validation;
- avatar-key contract and server-side allowlist/parity seam, without visual selection;
- server-side completion endpoint;
- real Home seed;
- Search, Clock, Weather, and Bookmarks seed records;
- idempotence;
- retry;
- concurrency;
- partial-state recovery;
- unique-conflict recovery;
- completion guard;
- `onboardingCompleted` only after the full seed exists and validates.

Does not include the final onboarding form, visual avatar picker, geolocation UI, final timezone/location interaction, or final visual experience.

### Checkpoint 4B — Onboarding UI + avatar/location/timezone UX

Includes:

- real onboarding page/form;
- visual avatar selection using owner-provided 8-bit assets;
- timezone detection/correction UX;
- browser geolocation after explicit action;
- location skip/denial/failure UX;
- loading/error/success states;
- keyboard/touch accessibility;
- UI connection to the 4A completion endpoint.

The avatar asset gate blocks 4B, not server-side 4A work. Preserve one review checkpoint after 4A completes and another review checkpoint after 4B completes before continuing to the next Phase 0002 block.

## 1. PocketBase compatibility validation

- [x] **1.1 Validate the PocketBase 0.40.3 compatibility contract in an isolated spike before any production implementation depends on it.** Objective: use a checksum-verified PocketBase 0.40.3 binary, a random loopback port, and disposable directories to prove the official compatible JavaScript SDK version; generated `pb_migrations` syntax; auth-collection field APIs; collection access rules; owner and effective dashboard-relation protections; SQLite indexes/partial unique constraints; supported request hooks; native transaction/batch behavior; concurrent request behavior; migration reversal; and safe setup/teardown that cannot target developer or production data. Record exact supported/unsupported findings and the chosen SDK version. **Probable files:** `tests/integration/pocketbase/compatibility.test.ts`, `openspec/changes/phase-0002-auth-onboarding/compatibility-evidence.md`, `package.json`, `pnpm-lock.yaml`. **Dependency:** Human delivery decision; no prior implementation task. **Completion criteria:** The harness proves every required surface against 0.40.3, cleans up only its own data/processes, and documents reproducible commands/results; no implementation depends on an untested assumption. If any design dependency is unsupported or cannot be proven, stop that implementation path and update the design/spec only as needed for the contradiction, then obtain owner approval before changing the architecture. **Tests/validation:** PASS — `pnpm vitest run tests/integration/pocketbase/compatibility.test.ts --reporter=verbose`, `pnpm typecheck`, `pnpm lint`, and `pnpm test`; evidence recorded in `compatibility-evidence.md`. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

## 2. Schema and migrations

- [x] **2.1 RED — Specify executable migration assertions for the approved users and onboarding collection storage contract.** Objective: encode expected auth fields, `user_preferences`, `dashboards`, `dashboard_widgets`, nullable layouts, default values, relation behavior, seed identities, and exact index names/semantics before writing the migration. **Probable files:** `tests/integration/pocketbase/migrations/auth-onboarding.test.ts`, `tests/integration/pocketbase/support/fixtures.ts`. **Dependency:** 1.1 confirms the runtime APIs and harness. **Completion criteria:** Assertions fail against an empty 0.40.3 database because the Phase 0002 schema is absent, and they distinguish required fields/constraints from future widget/grid fields. **Tests/validation:** Execute the new disposable migration-contract suite against a fresh database and confirm intentional RED failures. **gentle-ai review before continuing:** No. <!-- sdd-owner: implementation -->

- [x] **2.2 GREEN — Add the reviewed reversible PocketBase migration for the minimal auth/profile and onboarding data model.** Objective: configure the existing `users` auth collection; create only the three approved owner-scoped collections; add bounded fields, non-cascading relations, defaults, partial seed uniqueness, and supporting owner indexes; disable unapproved public auth flows/settings when 1.1 verifies the mechanism. **Probable files:** `pb_migrations/<timestamp>_auth_onboarding.js`, `tests/integration/pocketbase/migrations/auth-onboarding.test.ts`. **Dependency:** 2.1 RED assertions and 1.1 syntax confirmation. **Completion criteria:** Fresh and pre-provisioned-user databases migrate successfully; `Home` is identified by seed key rather than name; widget type remains non-unique; no bookmarks/search-engine/grid/provider schema is introduced; reverse behavior is explicitly safe for disposable data. **Tests/validation:** Run migration assertions, inspect SQLite indexes through the harness, apply migration twice as supported by PocketBase, and rehearse up/down/up only on disposable data. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

- [x] **2.3 TRIANGULATE/REFACTOR — Harden migration failure and recovery diagnostics without weakening data safety.** Objective: make incompatible pre-existing collections, duplicate/corrupt seed identities, and destructive reversal conditions fail explicitly for operator review. **Probable files:** `pb_migrations/<timestamp>_auth_onboarding.js`, `tests/integration/pocketbase/migrations/auth-onboarding.test.ts`, `docs/development/`. **Dependency:** 2.2 passing migration contract. **Completion criteria:** Migration errors do not erase users or valued records, reversible behavior is clear for disposable databases, and production rollback requires backup/operator authorization rather than an automatic destructive down migration. **Tests/validation:** Exercise incompatible schema and duplicate-data fixtures, assert sanitized diagnostics, and repeat disposable recovery rehearsal. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

## 3. Access rules and isolation tests

- [x] **3.1 RED — Create a direct normal-user PocketBase isolation matrix for all onboarding collections and user profile mutation.** Objective: define two-user positive and adversarial tests for list, view, create, update, and delete; forged/omitted owner; ownership reassignment; cross-owner dashboard relations; auth-field mutation; and blocked public account-management flows. **Probable files:** `tests/integration/pocketbase/isolation/auth-onboarding.test.ts`, `tests/integration/pocketbase/support/provisioning.ts`. **Dependency:** 2.2 migrated disposable database. **Completion criteria:** Tests authenticate only normal provisioned users for assertions and fail until approved rule/hook behavior exists; privileged setup is isolated from test actions. **Tests/validation:** Run the direct API matrix and assert filtered lists, inaccessible records, and unchanged victim records rather than relying on a single status code. **gentle-ai review before continuing:** No. <!-- sdd-owner: implementation -->

- [x] **3.2 GREEN — Implement PocketBase access rules and request hooks that enforce owner, immutable seed, profile allowlist, and effective dashboard-owner invariants.** Objective: make direct normal PocketBase calls independently enforce every collection rule and reject both old and proposed cross-owner relations. **Probable files:** `pb_migrations/<timestamp>_auth_onboarding.js`, `pb_hooks/auth_onboarding.pb.js`, `pb_hooks/lib/onboarding-validation.js`, `tests/integration/pocketbase/isolation/auth-onboarding.test.ts`. **Dependency:** 3.1 RED matrix and 1.1 hook/rule syntax confirmation. **Completion criteria:** Normal users cannot enumerate or mutate another owner’s records, cannot forge/reassign owner or seed identity, cannot attach a widget to another owner’s dashboard, and cannot mutate protected auth fields; authenticated own-record controls pass. **Tests/validation:** Pass the full direct API matrix including same-owner relation-change positive controls and resulting-record relationship tampering. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

- [x] **3.3 TRIANGULATE — Prove unique-constraint and cross-owner defenses under concurrent normal-user requests.** Objective: ensure rule behavior and durable constraints remain correct with independent connections rather than a single sequential client. **Probable files:** `tests/integration/pocketbase/isolation/auth-onboarding.test.ts`, `tests/integration/pocketbase/support/`. **Dependency:** 3.2 passing sequential isolation matrix. **Completion criteria:** Parallel forged-owner/relation operations leave no unauthorized record, while valid same-owner operations remain usable; unique collisions are classified without leaking SQL or auth details. **Tests/validation:** Execute parallel direct requests with two normal tokens and inspect persisted rows/index outcomes. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

## 4. Request-scoped PocketBase server boundary

- [x] **4.1 RED — Add unit and route-level tests for private origin/cookie configuration, fresh request clients, and no shared auth state.** Objective: define the server boundary’s configuration parsing, per-H3-event auth-store construction, event-local memoization, request auto-cancellation policy, token-size bound, DTO projection, and 401-versus-503 behavior. **Probable files:** `tests/unit/session.test.ts`, `tests/unit/app-origin.test.ts`, `tests/server/pocketbase-client.test.ts`, `server/utils/pocketbase.ts`. **Dependency:** 1.1 confirms the SDK/auth-store/refresh API and 3.2 proves direct isolation. **Completion criteria:** Tests fail before the boundary exists and cover separate User A/User B events, malformed configuration, invalid credentials, and upstream unavailability without a stale identity. **Tests/validation:** Run focused Vitest RED tests; preserve existing `tests/pocketbase-config.test.ts` expectations. **gentle-ai review before continuing:** No. <!-- sdd-owner: implementation -->

- [x] **4.2 GREEN — Implement private app-origin/cookie-mode validation and a request-scoped PocketBase client factory.** Objective: retain the Foundation endpoint parser while adding validated `NUXT_APP_ORIGIN` and `NUXT_SESSION_COOKIE_MODE`, fresh in-memory SDK auth stores, event-only memoization, and no module-global mutable client/auth state. **Probable files:** `nuxt.config.ts`, `.env.example`, `server/plugins/pocketbase-config.ts`, `server/utils/pocketbase.ts`, `server/utils/pocketbase-client.ts`, `tests/unit/`, `tests/server/`. **Dependency:** 4.1 RED tests. **Completion criteria:** The endpoint remains private, development HTTP mode is explicitly refused outside Nuxt development, secure mode is default, startup performs no new network I/O, and every request receives independent SDK/auth state. **Tests/validation:** Pass focused tests plus existing PocketBase configuration tests, lint, and typecheck. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

- [x] **4.3 REFACTOR — Centralize sanitized API error classification and server-only operational logging.** Objective: give routes one typed path for invalid input, invalid credential, outage, conflict, and throttling responses without serializing SDK errors, bodies, locations, cookies, passwords, or tokens. **Probable files:** `server/utils/api-error.ts`, `server/utils/pocketbase-client.ts`, `tests/unit/api-error.test.ts`, `tests/server/`. **Dependency:** 4.2 boundary passes. **Completion criteria:** Error DTOs conform to `{error:{code,message,fields?}}`, sensitive values are absent from messages/log records, and no client-visible response contains raw PocketBase payloads. **Tests/validation:** Sentinel-secret tests inspect returned JSON and captured logs for leakage. **gentle-ai review before continuing:** No. <!-- sdd-owner: implementation -->

## 5. Session cookie and auth utilities

- [x] **5.1 RED — Define cookie lifecycle and same-origin tests before implementing session resolution.** Objective: capture `__Host-kunai_session` production attributes, explicit development cookie naming, remaining-token lifetime, exact cookie clearing scope, refresh-on-protected-request behavior, safe outage retention, Origin/Fetch-Metadata rejection ordering, JSON content type, and 16 KiB body limit. **Probable files:** `tests/unit/session-cookie.test.ts`, `tests/unit/same-origin.test.ts`, `tests/server/session.test.ts`. **Dependency:** 4.2 request-scoped client factory. **Completion criteria:** Tests fail without session utilities and demonstrate that rejected unsafe requests mutate neither cookie nor PocketBase state. **Tests/validation:** Run focused Vitest RED suite with valid, missing, null, malformed, and cross-origin metadata cases. **gentle-ai review before continuing:** No. <!-- sdd-owner: implementation -->

- [x] **5.2 GREEN — Implement session resolution, cookie rotation/clearing, and same-origin protection.** Objective: resolve one session per event via `users.authRefresh()`, project only `{id, displayName, avatarKey, onboardingCompleted}`, distinguish invalid from unavailable, and protect every unsafe route before body processing or refresh. **Probable files:** `server/utils/session.ts`, `server/utils/same-origin.ts`, `server/utils/pocketbase-client.ts`, `tests/unit/`, `tests/server/`. **Dependency:** 5.1 RED suite and 1.1 refresh confirmation. **Completion criteria:** Tokens exist only in HttpOnly cookies, valid protected requests rotate their bounded cookie, invalid credentials clear it, upstream failures return 503 and retain it, and no global session state exists. **Tests/validation:** Pass cookie/origin tests, simultaneous event tests, and sentinel inspection of DTOs, headers, and logs. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

- [x] **5.3 TRIANGULATE — Verify server-side session refresh races and request isolation.** Objective: prove overlapping server-side SSR/API refreshes keep request/event identity isolated, rotated tokens correctly scoped, 401 invalid-session cleanup bounded to the affected request, 503 upstream-unavailable behavior retaining retryable cookies, and no stale server identity after invalid or unavailable refresh. **Probable files:** `tests/server/session.test.ts`, `tests/ssr/session-race.test.ts`, `server/utils/session.ts`. **Dependency:** 5.2 passing session behavior. **Completion criteria:** Multiple valid rotated tokens remain correctly scoped per request/event; concurrent User A/User B refreshes cannot exchange identity; invalid credentials clear only the affected session; upstream failures return 503 without serving stale identity. **Tests/validation:** Execute controlled overlapping server-side refresh tests against the disposable PocketBase harness. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

## 6. Login, session, and logout API

- [x] **6.1 RED — Add thin HTTP-contract tests for login, session, and logout routes.** Objective: specify bounded inputs/outputs, generic failures, origin/content-type/body enforcement, throttling response handling, cookie behavior, and 204 logout semantics. **Probable files:** `tests/server/auth-routes.test.ts`, `server/api/auth/login.post.ts`, `server/api/auth/session.get.ts`, `server/api/auth/logout.post.ts`. **Dependency:** 5.2 session utilities. **Completion criteria:** Tests fail before routes exist and assert no bearer credential/raw auth result appears in a response. **Tests/validation:** Run route RED suite with fake/controlled request-scoped PocketBase adapter cases. **gentle-ai review before continuing:** No. <!-- sdd-owner: implementation -->

- [x] **6.2 GREEN — Implement bounded login, session, and logout routes using normal user identity only.** Objective: authenticate email/password through Nuxt, establish/clear only the application cookie, return the safe session envelope, and avoid registration/recovery/social endpoints or superuser runtime paths. **Probable files:** `server/api/auth/login.post.ts`, `server/api/auth/session.get.ts`, `server/api/auth/logout.post.ts`, `server/utils/session.ts`, `tests/server/auth-routes.test.ts`. **Dependency:** 6.1 RED tests. **Completion criteria:** Login routes by returned onboarding state at the client layer, logout succeeds even when PocketBase is unavailable after origin validation, and all mutations are same-origin protected. **Tests/validation:** Pass route tests for valid/invalid login, 400/401/403/415/429/503 paths, cookie scope, and response/log secret sentinels. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

## 7. SSR restoration and routing

- [x] **7.1 RED — Define SSR and client-navigation state-machine tests for `/login`, `/onboarding`, and `/`.** Objective: prove redirects happen before protected HTML, original-event Set-Cookie propagation works, hydration uses the safe DTO, unavailable sessions render no protected content, and all authenticated responses are private/no-store. **Probable files:** `tests/ssr/auth-routing.test.ts`, `tests/*.nuxt.test.ts`, `app/app.vue`. **Dependency:** 6.2 API contracts. **Completion criteria:** Tests fail before routing/middleware exists and cover anonymous, incomplete, complete, expired, and unavailable states on SSR and navigation. **Tests/validation:** Boot Nuxt under test and inspect HTML, headers, redirects, and hydration state. **gentle-ai review before continuing:** No. <!-- sdd-owner: implementation -->

- [x] **7.2 GREEN — Add original-event SSR session initialization, global routing middleware, and client session composable.** Objective: resolve auth before page setup, use only safe Nuxt state, clear visible state on 401, suppress stale client session responses after overlapping login/logout/session calls, retry transient failures without stale protected data, and avoid an internal SSR API fetch that loses Set-Cookie. **Probable files:** `app/app.vue`, `app/plugins/session.server.ts`, `app/middleware/auth.global.ts`, `app/composables/useSession.ts`, `server/utils/session.ts`, `tests/ssr/`, `tests/*.nuxt.test.ts`. **Dependency:** 7.1 RED suite. **Completion criteria:** SSR/client routes follow the approved three-state table with no return URL feature, client SDK, local storage, protected-content flash, or stale client identity repaint after logout/401/outage. **Tests/validation:** Pass SSR state matrix, hydration parity, 401 navigation, stale client-response suppression, no-store header, and simultaneous User A/User B SSR tests. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

## 8. Profile and onboarding persistence (Checkpoint 4A)

- [x] **8.1 RED — Define pure onboarding input, location, and seed-payload validation tests.** Objective: specify exact parsing for display name, approved avatar key, IANA timezone, discriminated `defaultLocation`, unknown fields, bounds, and the four placeholder config payloads. **Probable files:** `shared/types/auth.ts`, `shared/types/onboarding.ts`, `shared/validation/onboarding.ts`, `tests/unit/onboarding-validation.test.ts`. **Dependency:** 2.2 schema contract and 7.2 safe session state. **Completion criteria:** Tests reject hidden timezone fallbacks, manual coordinate requirements, invalid coordinates, unrecognized fields, and location labels represented as geocoded data. **Tests/validation:** Run focused RED unit suite with valid null/label/coordinates cases and malformed JSON cases. **gentle-ai review before continuing:** No. <!-- sdd-owner: implementation -->

- [x] **8.2 GREEN — Implement shared pure DTO parsers and bounded persisted onboarding draft retrieval.** Objective: provide no-`any` validators and `GET /api/onboarding` that expose only incomplete users’ persisted profile/preferences draft without writing or exposing internal auth data. **Probable files:** `shared/types/auth.ts`, `shared/types/onboarding.ts`, `shared/validation/onboarding.ts`, `server/api/onboarding/index.get.ts`, `server/utils/onboarding.ts`, `tests/unit/`, `tests/server/onboarding-routes.test.ts`. **Dependency:** 8.1 RED tests and 3.2 access rules. **Completion criteria:** Unknown or invalid values receive field-level errors; complete users receive a bounded conflict; missing partial data maps explicitly to empty/null draft state. **Tests/validation:** Pass parser and GET route tests for anonymous, incomplete, complete, malformed stored state, and no secret exposure. **gentle-ai review before continuing:** No. <!-- sdd-owner: implementation -->

## 9. Avatar registry integration (4A contract, 4B asset gate)

- [x] **9.1 Prepare the avatar registry contract and parity-test seam without inventing an avatar.** Objective: define the immutable `{key, src, label}` manifest interface, root-relative path constraints, unknown-key behavior, and a PocketBase-compatible manifest/fixture format that can be validated in both runtimes. This is the Checkpoint 4A avatar-key contract only; it does not render selectable product avatars. **Probable files:** `shared/avatars.ts`, `shared/types/onboarding.ts`, `pb_hooks/lib/avatar-manifest.js`, `tests/unit/avatar-registry.test.ts`, `tests/integration/pocketbase/avatar-parity.test.ts`, `public/avatars/README.md`. **Dependency:** 8.1 validation contract and 1.1 hook runtime findings. **Completion criteria:** The contract has zero fabricated selectable product entries, no upload path, and fixture-only tests prove duplicate-key rejection, unsafe-path rejection, unknown-key behavior, and shared/PocketBase allowlist parity without requiring owner assets. **Tests/validation:** Verify duplicate keys, unsafe paths, unknown persisted keys, and registry/PocketBase allowlist parity using clearly separated fixtures only. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

- [x] **9.2 Integrate owner-supplied avatar assets and stable keys, then enable final avatar selection validation.** Objective: populate the approved manifest and bundled assets from the owner-provided files and stable keys without adding UI until Checkpoint 4B is authorized. **Probable files:** `public/avatars/*`, `shared/avatars.ts`, `pb_hooks/lib/avatar-manifest.js`, `tests/unit/avatar-registry.test.ts`, `tests/integration/pocketbase/avatar-parity.test.ts`. **Dependency:** Owner has supplied assets and stable keys; 9.1 contract complete. **Completion criteria:** Every displayed asset maps to one stable approved key, both runtimes accept exactly the same keys, unknown keys produce an explicit unavailable state, and arbitrary uploads remain absent. **Tests/validation:** PASS — manifest parity and asset-path tests pass against the owner-supplied `avatar-01` through `avatar-10` PNG assets; manual tablet/desktop visual inspection is accepted as part of Checkpoint 4B validation when the real selector exists. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

## 10. Location/timezone onboarding UX (Checkpoint 4B)

- [x] **10.1 RED — Create component tests for correctable timezone and explicit geolocation lifecycle.** Objective: specify client-only timezone detection when no persisted value exists; accessible form feedback; explicit-action-only geolocation; secure-context/unavailable/denied/timeout/failure paths; optional non-geocoded label; clear/skip behavior; and no coordinate entry requirement. **Probable files:** `tests/onboarding.nuxt.test.ts`, `app/composables/useOnboarding.ts`, `app/pages/onboarding.vue`. **Dependency:** 8.2 draft/validation behavior; 9.2 is required only for enabled avatar-selection tests. **Completion criteria:** Tests fail before UI implementation and prove no automatic geolocation call, no hidden UTC fallback, and retained in-memory form data after recoverable failure. **Tests/validation:** RED — `pnpm vitest run tests/onboarding.nuxt.test.ts --reporter=verbose` fails because the 10.2 onboarding form, avatar picker, timezone input, geolocation controls, location controls, submit/loading/error/retry controls, and keyboard/touch controls do not exist yet; `pnpm lint`, `pnpm typecheck`, and `git diff --check` pass. **gentle-ai review before continuing:** No. <!-- sdd-owner: implementation -->

- [x] **10.2 GREEN — Implement the accessible onboarding form and connect it to the 4A completion endpoint.** Objective: render correctable timezone, location detection/clear/optional-label controls, final avatar picker when 9.2 is unblocked, submit wiring, validation/live status/focus handling, loading/retry behavior, success/error states, and keyboard/touch-safe interaction without provider calls. **Probable files:** `app/pages/onboarding.vue`, `app/composables/useOnboarding.ts`, `app/composables/useSession.ts`, `app/components/AvatarPicker.vue`, `app/components/ui/*`, `tests/onboarding.nuxt.test.ts`. **Dependency:** 10.1 RED tests and 9.2 for rendering a selectable avatar picker in a completable flow. **Completion criteria:** Denied or unavailable geolocation remains non-blocking, labels are explicitly non-geocoded, no browser data is persisted outside approved submit operations, and no weather/geocoding/search provider is added. **Tests/validation:** PASS — `pnpm vitest run tests/onboarding.nuxt.test.ts --reporter=verbose` (13 tests), `pnpm lint`, `pnpm typecheck`, `pnpm test` (211 tests after one flaky concurrency retry), `pnpm build`, `git diff --check`, and the 9.2 desktop/tablet avatar visual inspection recorded in `checkpoint-4b-evidence.md`. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

## 11. Idempotent Home seed (Checkpoint 4A)

- [x] **11.1 RED — Build an integration test matrix for empty and partial onboarding seed recovery.** Objective: express expected reusable IDs/counts/configs for no state, profile-only, preferences-only, Home-only, and each missing-widget state; include completed replay preservation and no Travel/Dev/layout creation. **Probable files:** `tests/integration/pocketbase/onboarding-seed.test.ts`, `tests/integration/pocketbase/support/fixtures.ts`, `tests/server/onboarding-complete.test.ts`. **Dependency:** 2.2 migration, 3.2 rules, and 8.2 validation. **Completion criteria:** Tests fail before seed service exists and assert exactly one `home` seed and four fixed widget seed keys/types with null layouts and approved placeholder configs. **Tests/validation:** Run RED cases against fresh/partial disposable databases with normal user credentials. **gentle-ai review before continuing:** No. <!-- sdd-owner: implementation -->

- [x] **11.2 GREEN — Implement authenticated load-or-create seed operations with durable conflict recovery.** Objective: ensure preferences, Home, and four seed records in fixed order using owner-derived identities; reload exact winners after unique conflicts; preserve valid IDs; reject incompatible/corrupt records rather than deleting or relabeling them. **Probable files:** `server/utils/onboarding.ts`, `server/api/onboarding/complete.post.ts`, `tests/integration/pocketbase/onboarding-seed.test.ts`, `tests/server/onboarding-complete.test.ts`. **Dependency:** 11.1 RED matrix and 1.1 confirmed unique/batch semantics. **Completion criteria:** Retry converges without a read-then-create-only defense; config matches the validated snapshot; layouts remain `null`; no dashboard editor, functional widget, bookmark record, or provider call is added. **Tests/validation:** PASS — `pnpm vitest run tests/integration/pocketbase/onboarding-seed.test.ts --reporter=verbose`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `git diff --check`. Empty, partial, replay, corrupt-seed, customization-preservation, and retry-idempotence cases pass against disposable PocketBase. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

## 12. Onboarding retry, concurrency, and partial failure (Checkpoint 4A)

- [x] **12.1 RED — Define failure-injection and concurrent-completion integration tests for truthful finalization.** Objective: test failure after each durable stage, token expiry between stages, lost success response, unique/busy conflicts, simultaneous identical/conflicting submissions, direct premature completion, and concurrent deletion/mutation. **Probable files:** `tests/integration/pocketbase/onboarding-concurrency.test.ts`, `tests/server/onboarding-complete.test.ts`, `tests/integration/pocketbase/support/failure-injection.ts`. **Dependency:** 11.2 seed recovery succeeds sequentially. **Completion criteria:** Tests fail until final batch and PocketBase transactional completion guard exist; expected winner/snapshot, marker, IDs, and rollback outcomes are explicit. **Tests/validation:** Run independent-client concurrent tests and verify persisted state directly after every injected failure. **gentle-ai review before continuing:** No. <!-- sdd-owner: implementation -->

- [x] **12.2 GREEN — Implement bounded final batch, transactional PocketBase completion guard, and retry policy.** Objective: atomically apply the final preferences/profile/placeholder snapshot and completion marker, reuse event-bound transaction context, reject corrupt same-key data and completed-profile rewrites, and retry only confirmed retryable conflicts a bounded number of times. **Probable files:** `server/utils/onboarding.ts`, `server/api/onboarding/complete.post.ts`, `pb_hooks/auth_onboarding.pb.js`, `pb_hooks/lib/onboarding-validation.js`, `tests/integration/pocketbase/onboarding-concurrency.test.ts`. **Dependency:** 12.1 RED tests and 1.1 proven transaction/batch/hook semantics. **Completion criteria:** Completion is set only when the full predicate is true in the same transaction; failed finalization rolls back its snapshot; first successful conflicting finalization wins; no in-process lock, superuser CRUD, or Nuxt-only authorization substitutes for the guard. **Tests/validation:** PASS — `pnpm vitest run tests/integration/pocketbase/onboarding-concurrency.test.ts --reporter=verbose`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `git diff --check`. Concurrency, busy/unique recovery, rollback/no-false-completion, direct premature completion guard, concurrent deletion/mutation, lost-response replay, and retry after valid partial/transient failure pass. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

- [x] **12.3 REFACTOR — Stabilize recoverable onboarding conflict responses and API retry/resume behavior.** Objective: map seed conflicts, exhausted retries, expiry, and transient failures to bounded user-safe route results while allowing GET/refresh to discover a competing completed result. This remains Checkpoint 4A server/API behavior, not the final onboarding UI. **Probable files:** `server/utils/onboarding.ts`, `server/utils/api-error.ts`, `tests/server/onboarding-complete.test.ts`. **Dependency:** 12.2 atomic persistence passes. **Completion criteria:** The API never reports false completion; valid persisted partial data is reusable; a completed replay is read-only and does not overwrite customization; error messages expose no internal schema or credentials. **Tests/validation:** Pass route retry and lost-response scenarios with sanitized response assertions. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

## 13. Protected minimal Home landing

- [x] **13.1 RED — Add Home endpoint and landing tests for completed, incomplete, and inconsistent seed state.** Objective: specify `GET /api/home` and protected UI behavior that confirms the private initialized Home without rendering widgets or dashboard controls. **Probable files:** `tests/server/home.test.ts`, `tests/ssr/auth-routing.test.ts`, `tests/home.nuxt.test.ts`, `server/api/home.get.ts`, `app/pages/index.vue`. **Dependency:** 12.2 truthful completion and 7.2 routing. **Completion criteria:** Tests fail before endpoint/page implementation and assert 401, onboarding-required/inconsistent conflict, private/no-store behavior, and no future-phase controls. **Tests/validation:** RED confirmed — `pnpm vitest run tests/server/home.test.ts tests/home.nuxt.test.ts --reporter=verbose` fails 12 contract tests: all `GET /api/home` cases fail because `server/api/home.get.ts` is absent, while the landing cases fail because the current placeholder lacks the authenticated-user/minimal-Home/logout contract and still serializes the safe session. `pnpm vitest run tests/ssr/auth-routing.integration.nuxt.test.ts --reporter=verbose` fails the completed-session minimal-Home case because the SSR HTML lacks `Log out`; the other four routing-state cases pass. `pnpm lint`, `pnpm typecheck`, and `git diff --check` pass. **gentle-ai review before continuing:** No. <!-- sdd-owner: implementation -->

- [x] **13.2 GREEN — Implement the minimal protected Home confirmation and logout affordance.** Objective: display only the authenticated user’s initialized Home name/state and an accessible logout action while preserving the shared visual baseline and main landmark. **Probable files:** `server/api/home.get.ts`, `app/pages/index.vue`, `app/app.vue`, `tests/server/home.test.ts`, `tests/home.nuxt.test.ts`, `tests/theme.nuxt.test.ts`. **Dependency:** 13.1 RED tests. **Completion criteria:** No tab lifecycle, edit mode, grid, widget rendering, provider behavior, or Settings shell appears; existing appearance-control behavior remains covered. **Tests/validation:** GREEN verified — `pnpm vitest run tests/server/home.test.ts tests/home.nuxt.test.ts tests/ssr/auth-routing.integration.nuxt.test.ts --reporter=verbose` passed 17 tests; `pnpm lint`, `pnpm typecheck`, `pnpm build`, `git diff --check`, and subsequent full `pnpm test` passed 223 tests. A prior full-suite run hit a historical onboarding-concurrency flake in `tests/integration/pocketbase/onboarding-concurrency.test.ts`; diagnosis did not reproduce it, found no evidence that 13.2 caused it, and did not declare it resolved. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

## 14. Security tests

- [x] **14.1 TRIANGULATE — Add end-to-end server security regression coverage across session, routes, rules, and SSR output.** Objective: combine two-user isolation, CSRF/login-CSRF/logout-CSRF, token sentinel inspection, sensitive-field mass assignment, cache headers, expiry/outage, cross-request identity, and no client SDK/local-storage leakage. **Probable files:** `tests/server/security.test.ts`, `tests/ssr/security.test.ts`, `tests/integration/pocketbase/isolation/auth-onboarding.test.ts`, `tests/integration/pocketbase/onboarding-concurrency.test.ts`. **Dependency:** 13.2 implemented protected flow and all prior security boundaries. **Completion criteria:** Every approved threat has a positive/negative observable assertion; direct API assertions use normal tokens; browser/session output never contains secret sentinel values. **Tests/validation:** Run the full targeted security suite plus static inspection of public runtime config, browser payloads, storage, and captured logs. **Evidence:** PASS — new real-PocketBase H3, direct normal-token isolation, and Nuxt SSR/static leakage coverage passed for CSRF variants, cookie security shapes, token/body sentinels, route mass assignment, Home invalid/outage distinction, cross-request identity, two-user record isolation, SSR protected-content suppression, and browser/runtime leakage checks. DEFECT — `tests/server/security.test.ts` proves `GET /api/auth/session` returns valid, invalid, and outage responses without required `Cache-Control: private, no-store`; 14.2 must address only this evidenced defect. FLAKE — no new flake observed; `tests/integration/pocketbase/onboarding-concurrency.test.ts` passed and was not changed because its historical conflicting-submit reason was not reproduced by this task. MANUAL — real browser/HTTPS/proxy verification remains Task 15.1. Validation: targeted security suite passes with the demonstrated cache-header defect captured as an expected-failing assertion; onboarding concurrency suite passed 33 tests; `pnpm lint`, `pnpm typecheck`, and `git diff --check` passed. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

- [x] **14.2 REFACTOR — Address only evidenced security-test defects and remove duplication without changing the approved boundary.** `GET /api/auth/session` now sets `Cache-Control: private, no-store` before session resolution, so valid, invalid/expired, and PocketBase-outage responses consistently retain the required private cache policy. This corrects only the defect demonstrated by 14.1; auth model, cookie strategy, PocketBase rules, and other production behavior are unchanged. The 14.1 expected-failing assertion is now a normal passing assertion. **Tests/validation:** PASS — affected security/auth/session/home/onboarding suites passed 100 tests; full `pnpm test` passed 239 tests; `pnpm lint`, `pnpm typecheck`, `pnpm build`, and `git diff --check` passed. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

## 15. Browser and manual validation

- [x] **15.1 Perform the documented manual browser, cookie, and available HTTPS/proxy validation matrix.** Objective: validate real browser development-cookie behavior, automated production-cookie flag/configuration behavior, SSR refresh, logout, stale tabs, two profiles, denied/unavailable geolocation, keyboard/tablet touch use, reduced motion, token/private-config non-exposure, and proxy requirements. **Probable files:** `docs/development/`, `docs/architecture/`, manual evidence location agreed by repository convention. **Dependency:** 14.2 passing automated suite and owner-provided avatar assets for complete onboarding. **Completion criteria:** Manual/automated results identify browser/version/environment, expected/actual result, and any blocker; no claim of global token revocation or provider-backed weather is made. If a real HTTPS/reverse-proxy deployment is available during Phase 0002, validate against it; if not, document the deployment validation as pending for Hardening/release and do not weaken cookie/security configuration to satisfy local tests. **Tests/validation:** Execute the matrix against available safe environments, inspect `document.cookie`, storage, SSR HTML/payloads, headers, logs, and config exposure with a token sentinel. **gentle-ai review before continuing:** Yes. Evidence: `manual-browser-validation-evidence.md`; all locally executable cases passed after the Home confirmation blocker fix, and HTTPS/reverse-proxy remains explicitly PENDING for a real deployment. <!-- sdd-owner: implementation -->

## 16. ADR

- [x] **16.1 Create and review the accepted Nuxt/PocketBase session-boundary ADR from verified implementation evidence.** Objective: document the server-owned HttpOnly bearer cookie, fresh request auth stores, minimal DTO, normal-user PocketBase operations, defense-in-depth rules, admin provisioning, HTTPS/origin deployment prerequisites, outage behavior, and stateless logout limitation with alternatives and consequences. **Probable files:** `docs/decisions/0009-nuxt-pocketbase-session-boundary.md`, `docs/decisions/README.md`. **Dependency:** 1.1 compatibility result, 14.2 passing security suite, and 15.1 manual evidence. **Completion criteria:** ADR accurately reflects implemented/verified behavior, references ADRs 0002/0007/0008, and does not claim untested global revocation, privileged runtime access, or browser token access. **Tests/validation:** Cross-check ADR statements against code, automated evidence, and manual matrix; confirm index link resolves; `git diff --check` passed. **gentle-ai review before continuing:** Yes. ADR: `docs/decisions/0009-nuxt-pocketbase-session-boundary.md`; index: `docs/decisions/README.md`. <!-- sdd-owner: implementation -->

## 17. Documentation and context updates

- [x] **17.1 Update operational/setup documentation for the implemented Phase 0002 boundary and test harness.** Objective: document environment variables, account provisioning, pinned PocketBase/SDK compatibility result, migration/hook application order, disposable integration-test prerequisites, HTTPS/proxy requirements, backup/recovery expectations, and sanitized troubleshooting. **Probable files:** `.env.example`, `docs/development/local-setup.md`, `docs/architecture/configuration.md`, `docs/architecture/security.md`, `docs/architecture/data-model.md`. **Dependency:** 16.1 ADR and 15.1 evidence. **Completion criteria:** Instructions neither contain usable secrets nor direct operators to destructive live rollback; they distinguish normal user operations from setup-only administration. **Tests/validation:** PASS — `.env.example` contains only real Phase 0002 variables with local/placeheld values; variable names were cross-checked against implementation/tests; documented script names and referenced paths exist; internal ADR links resolve; `git diff --check` passed. Documentation now covers session/cookie modes, PocketBase 0.40.3 and SDK 0.28.1 compatibility, migration/hook order, batch prerequisite, administrative account provisioning, disposable integration harness, HTTPS/proxy pending validation, backup/rollback expectations, and sanitized troubleshooting. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

- [x] **17.2 Update project truth after Phase 0002 verification is accepted.** Objective: reflect actual formally accepted verification status and next allowed action without starting Phase 0003 or declaring archived work complete. **Probable files:** `project/context.md`, `project/phase-map.md`, relevant architecture/documentation cross-links. **Dependency:** Phase 0002 formally verified/accepted by `/sdd-verify`. **Completion criteria:** Context and phase map match the accepted verification state, preserve Phase 0003 as planned, and do not alter archived history. **Tests/validation:** PASS — accepted `/sdd-verify` result is `VERIFIED` with 18/18 requirements verified and no critical blockers. Phase 0002 implementation is complete, automated validation is complete, manual validation available in the current environment is complete, and the verification packet is complete. Phase 0002 is formally verified but not archived. Phase 0003 remains planned/not started; HTTPS/reverse-proxy real validation remains DEFERRED/PENDING for Hardening/Release; no archive, commit, push, or Phase 0003 work was performed. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

## 18. Final validation and SDD-verify preparation

- [x] **18.1 Run the complete automated quality and migration evidence suite from a clean disposable setup.** Objective: verify strict TDD evidence across unit, Nuxt component, route, SSR, real PocketBase migration/isolation/concurrency tests, plus lint, typecheck, build, and full test command. **Probable files:** `package.json`, `tests/`, `tests/integration/pocketbase/`, CI or documented test-command configuration if present. **Dependency:** 17.1 operational documentation. **Completion criteria:** All commands pass without silently skipping required integration prerequisites; failures are recorded and fixed through the appropriate earlier task rather than waived. **Tests/validation:** PASS — `pnpm lint`, `pnpm typecheck`, `pnpm test` (26 files, 242 tests), `pnpm build`, `git diff --check`, and the explicit disposable PocketBase integration command (7 files, 69 tests) passed. Required integration coverage included compatibility, schema/owner isolation, auth/onboarding isolation, onboarding seed, onboarding concurrency, batch preflight, and Home route against real PocketBase. Evidence: `final-automated-validation-evidence.md`. The historical onboarding concurrency flake was reproduced, diagnosed as a transient seed-read race during concurrent completions, fixed with bounded persisted-state re-read/validation while preserving real `seed_conflict`, and did not recur in the final post-fix repetitions. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

- [x] **18.2 Prepare the Phase 0002 SDD verification packet and stop before archive or Phase 0003.** Objective: assemble traceable requirement-to-evidence references, review checkpoints, manual results, migration rollback rehearsal, avatar-gate disposition, known limitations, and unresolved blockers for `/sdd-verify`. **Probable files:** `openspec/changes/phase-0002-auth-onboarding/`, verification evidence location agreed by repository convention. **Dependency:** 18.1 passing automated validation and human review of delivery units. **Completion criteria:** The packet makes it possible to assess every approved requirement without claiming archive status; archive and any Phase 0003 planning remain separate owner-authorized actions. **Tests/validation:** PASS — `verification-evidence.md` maps all 18 approved Phase 0002 requirements to implementation, tests, manual evidence where applicable, reviews, and status. Accepted `/sdd-verify` result: `VERIFIED`, 18/18 requirements verified, no critical blockers. HTTPS/reverse-proxy real deployment validation remains explicitly deferred to Hardening/Release and is not claimed as a real PASS. Phase 0002 remains active and unarchived; Phase 0003 remains unstarted; Task 17.2 is complete. **gentle-ai review before continuing:** Yes. <!-- sdd-owner: implementation -->

## Proposed gentle-ai checkpoints

Owner-approved checkpoints for Phase 0002 apply. At each checkpoint, stop and request authorization before continuing when gentle-ai review is required.

| Checkpoint | After task | Purpose |
|---|---:|---|
| PocketBase compatibility | 1.1 | Approve the exact PocketBase/SDK/hook/transaction mechanism or stop for design/spec/owner approval. |
| Schema + owner isolation | 2.2 and 3.3 | Review migrations, reversibility, direct normal-user rules, and relational ownership protection. |
| Auth/session boundary | 5.3, 6.2, and 7.2 | Review cookie/origin/request-isolation, auth APIs, and SSR routing before onboarding completion is exposed. |
| Checkpoint 4A — Onboarding persistence + atomic Home seed | 8.2, 9.1, 11.2, 12.2, and 12.3 | Review server-side onboarding validation/persistence, avatar-key contract, Home/Search/Clock/Weather/Bookmarks seed, idempotence, concurrency, partial failure, batch rollback, direct API completion guard, and retry/resume behavior before any final onboarding UI is exposed. |
| Checkpoint 4B — Onboarding UI + avatar/location/timezone UX | 9.2 and 10.2 | Review the real onboarding form, owner-supplied 8-bit avatar selection, timezone correction UX, explicit geolocation UX, loading/error/success states, accessibility, and wiring to the 4A completion endpoint before continuing to the next Phase 0002 block. |
| Security/manual validation | 14.2, 15.1, and 16.1 | Review adversarial results, available browser/manual evidence, deployment-validation disposition, and ADR accuracy before final validation. |
| Final verify/archive | 18.2 | Human review/delivery decision before `/sdd-verify`; archive and Phase 0003 remain separate owner-authorized actions. |

## Pending decisions and blocked work

| Item | Status / required action |
|---|---|
| Delivery chain strategy | Resolved: no chained PRs for Phase 0002. Work stays on `feat/phase-0002-auth-onboarding`, split by checkpoints and small coherent commits. No push, merge, or PR without explicit authorization. |
| Avatar assets and stable keys | Resolved for registry data: the owner supplied `avatar-01` through `avatar-10` and matching PNG assets. Checkpoint 4B visual avatar/onboarding UI remains unstarted and requires explicit authorization; arbitrary uploads remain out of scope. |
| PocketBase compatibility result | Task 1.1 is a hard implementation gate; unsupported migration/rule/hook/batch/transaction semantics require design/spec revision and owner approval before an architecture change. |
| HTTPS/proxy deployment details | Phase 0002 must implement secure development/production cookie behavior, automated checks for flags/configuration, documentation of HTTPS/reverse-proxy requirements, and token/private-config leakage checks. Manual validation against a real HTTPS/proxy deployment is required only if such an environment is available during Phase 0002; otherwise record it as pending for Hardening/release without weakening security for local tests. |
| Phase 0003 | Explicitly out of scope; no task starts dashboard tabs, lifecycle, navigation, or settings shell. |
