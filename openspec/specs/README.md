# OpenSpec Main Specs

This directory is intentionally empty at project start.

gentle-ai/OpenSpec merges accepted change specs into the main spec set as changes are archived.

Do not pre-populate this directory with future behavior and then describe it as implemented truth.

Future product direction lives in `docs/` until the relevant SDD change is applied, verified, and archived.
